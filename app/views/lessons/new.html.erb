<section class="py-5 text-center container">
  <div class="row py-lg-5">
    <div class="col-lg-6 col-md-8 mx-auto">
      <h1 class="fw-light">Create a new lesson</h1>
      <p class="lead text-muted">Are you a teacher? Create your lesson below:</p>
      <%= simple_form_for @lesson do |f| %>
        <%= f.input :name, placeholder: 'Intro to CSS' %>
        <%= f.input :description, placeholder: 'Setting classes & IDs' %>
        <%= f.input :price, label: 'Price per hour', placeholder: '€' %>
        <%= f.submit class: 'btn btn-primary my-2' %>
      <% end %>
    </div>
  </div>
</section>
